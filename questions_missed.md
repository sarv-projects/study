# Company 1: SoundHound

## Python Fundamentals — Revise These

- `dict` — keys, values, items, get, setdefault, defaultdict, Counter
- `tuple` — immutable, unpacking, namedtuple
- `list` — append, extend, sort, sorted, list comprehensions
- `set` — union, intersection, difference, membership check
- Functions: `map`, `filter`, `zip`, `enumerate`, `reversed`, `sorted` with key

## Key Reminder

In conversational AI / general AI — always ensure **proper authentication**, especially when dealing with sensitive areas or data.
